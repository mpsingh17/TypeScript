/*
function showTodo(todo: {title: string, text: string}) {
  console.log(todo.title + ': ' + todo.text);
}
let myTodo = {title: 'trash', text: 'Throw trash out'};

showTodo(myTodo);
*/
function showTodo(todo) {
    console.log(todo.title + ": " + todo.text);
}
var myTodo = { title: 'trash', text: 'Take it outside' };
showTodo(myTodo);
